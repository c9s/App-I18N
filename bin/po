#!/usr/bin/perl
use strict;
use warnings;
use lib 'lib';
use App::Po;
use App::Po::Command;
use App::Po::Command::Help;

my $cmd = shift @ARGV;
my @args = @ARGV;
App::Po::Command->invoke( $cmd , @args );
